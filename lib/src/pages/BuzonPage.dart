import 'package:flutter/material.dart';

class BuzonPage extends StatelessWidget {
  const BuzonPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child:Text("Buzón de peticiones al administrador")
    );
  }
}